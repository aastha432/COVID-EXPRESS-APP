 import React, { Component, PropTypes } from 'react';
 
//Orders have to be read from database.... API calls to do................

 class Orders extends Component {
     constructor(props) {
         super(props);
 
     }
 
 
     render() {
         return (
             <div className="container">
                    <h1>This is the orders page.</h1>
             </div>
         );
     }
 }
 
 
 export default Orders;